package view;

import javax.swing.*;

public class Beullog extends JPanel{
	 int size;
	   String map[][];

	   public Beullog() {
	      this.size = size;
	      map = new String[size][size];
	      this.setLayout(null);
	      this.setBounds(0, 0, 1100, 900);

	   }
}
